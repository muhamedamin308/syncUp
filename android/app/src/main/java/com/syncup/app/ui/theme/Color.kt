package com.syncup.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand ───────────────────────────────────────────────────────────────────
val SyncUpBlue        = Color(0xFF2563EB)   // Primary — sent message bubbles, buttons
val SyncUpBlueDark    = Color(0xFF1D4ED8)   // Pressed / dark variant
val SyncUpBlueLight   = Color(0xFFEFF6FF)   // Light tint for backgrounds

// ─── Neutrals ────────────────────────────────────────────────────────────────
val NeutralWhite      = Color(0xFFFFFFFF)
val NeutralSurface    = Color(0xFFF8FAFC)   // Screen backgrounds
val NeutralCard       = Color(0xFFFFFFFF)   // Card backgrounds
val NeutralBorder     = Color(0xFFE2E8F0)   // Dividers, borders
val NeutralMuted      = Color(0xFF94A3B8)   // Secondary text, timestamps
val NeutralText       = Color(0xFF1E293B)   // Primary text

// ─── Received bubble ─────────────────────────────────────────────────────────
val BubbleReceived    = Color(0xFFEFF6FF)    // Left-side message bubbles

// ─── Status indicators ───────────────────────────────────────────────────────
val OnlineGreen       = Color(0xFF22C55E)
val OfflineGrey       = Color(0xFFCBD5E1)

// ─── Dark theme equivalents ──────────────────────────────────────────────────
val DarkBackground    = Color(0xFF0F172A)
val DarkSurface       = Color(0xFF1E293B)
val DarkCard          = Color(0xFF293548)
val DarkBorder        = Color(0xFF334155)
val DarkMuted         = Color(0xFF64748B)
val DarkText          = Color(0xFFF1F5F9)
val DarkBubbleReceived = Color(0xFF1E3A5F)